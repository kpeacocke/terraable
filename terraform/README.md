# Terraform

MVP Terraform content for substrate provisioning and HCP Terraform handoff outputs.

## MVP target
- OpenShift substrate baseline (`modules/substrate_openshift`)

## Contract outputs
Terraform outputs should provide values used to build the canonical payload in `docs/handoff-contract.md`.
