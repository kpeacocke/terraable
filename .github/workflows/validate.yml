name: Validate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  repo-health:
    name: Repo health
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      - name: Verify required repository files
        run: |
          test -f README.md
          test -f CONTRIBUTING.md
          test -f SECURITY.md
          test -f CODEOWNERS
          test -f .editorconfig
          test -f .gitignore
          test -f .gitattributes

  terraform-fmt:
    name: Terraform fmt
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@1b616fd6ba9f3f9f8ff6b431728e5f202c3ae86c # v3

      - name: Check formatting
        run: |
          if find . -type f -name '*.tf' | grep -q .; then
            terraform fmt -check -recursive
          else
            echo "No Terraform files found; skipping."
          fi

  yamllint:
    name: YAML lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      - name: Lint YAML
        uses: ibiqlik/action-yamllint@d93e1c790db2b35546e6049c3895efaaee0e5ae4 # v3
        with:
          config_file: .yamllint.yml
